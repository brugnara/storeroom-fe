import { Box } from 'react-bulma-components';
import React from 'react';

export class CallToAction extends React.Component {
    render(): React.ReactNode {
        return <Box>call to action</Box>;
    }
}
